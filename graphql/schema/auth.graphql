type LoginPayload {
  accessToken: String!
  expiresIn: Time!
  refreshToken: String!
}

type RefreshAccessTokenPayload {
  accessToken: String!
  expiresIn: Time!
}

directive @auth on FIELD_DEFINITION
